"use client";

/**
 * No mobile o wallpaper está na própria hero (CSS) e sobe com o scroll nativo.
 * Este componente ficou sem lógica de sync — mantido só para não partir imports.
 */
export function WorldMobileWallpaperScroll() {
  return null;
}
